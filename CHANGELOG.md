# 更新日志

本项目的所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- **正交折线连接线**：line/arrow 新增 route 语义——端点变化（绑定节点移动、点编辑拖端点）自动重建 L 形中间路径，出入方向优先按绑定节点中心相对方位；纯函数 buildOrthoWaypoints 落在 geometry 层并补单测
- **连接器模式**：单选形状/文本/框架/图片时四边中点外侧显示锚点圆点，拖出画线落到目标节点即创建 route 绑定箭头（一步撤销），落到空白创建普通箭头；锚点随选中/工具/视口变化即时重建
- **ask_user 澄清工具**（交流模式）：模型可在需求多义时向用户提问——面板内展示问题 + 候选点选 + 自由输入，等待回答后作为工具结果回传继续对话；支持跳过与请求中断
- **draw_flowchart 布局参数**：新增 direction（TB 自上而下 / LR 自左向右）与 hGap/vGap 间距控制；连线升级为 route 正交折线并绑定节点
- **增量画布感知**：`get_canvas` 全量调用三级响应——画布未变化时只返回「无变化」简短摘要（附实时视口）；有变化时默认只返回增量变更集（新增 / 更新元素完整数据带 change 标记 + 删除 id 清单，归一化指纹规避浮点抖动误报）；变更面过大或传 `full:true` 时退回完整快照。长对话节省 50%+ token；定向查询（ids/bounds/viewport）不读不写缓存
- **智能对齐参考线**：拖动单元素时，期望包围盒的边线 / 中心与其它元素（含锁定）对应线距离 ≤6px 即吸附并显示品红参考线（sky 层，缩放下手感恒定）；对齐命中覆盖同轴网格吸附，约束夹紧仍最优先；候选每手势收集一次，拖动高频调用零额外开销
- **落框高亮**：拖动中移动集合联合 bbox 完全落入某框架（含描边容差，嵌套取最小面积）时高亮预判归属——高亮画在 sky 层覆盖框，不改框架本体属性、无历史污染
- **框架名称标签**：有 name 的框架在上沿外挂名称（点击穿透选中整框），随折叠自然隐藏；`get_canvas` 输出补 name 字段供模型按名引用
- **多开写入竞争防护**：桌面端写场景/索引前比对文件 mtime 与本地基线，被其他窗口或程序改过即拒绝覆盖并 toast 提示——消除双开时"后写无声覆盖前写"的丢数据风险；判定逻辑抽为纯函数并补测试
- **滚动备份 ×5**：场景文件的原子写入升级为保留最近 5 代备份（bak.1 最新），载入按 主文件 → bak.1..N 回退，误删画布可回溯更远
- **AI 面板懒加载**：panel/tools/client 约 2900 行改为首次唤起时动态加载，主包体积 633KB → 584KB（gzip 199→186KB）
- 新增 `ARCHITECTURE.md`：分层设计、page/world 双坐标基准与契约元素坐标约定等关键陷阱文档化
- **Alt+拖拽复制（落点克隆）**：按住 Alt 拖动元素，松手后在拖动前原位重建副本——移动中的元素即副本，支持编辑器拖动与框内空白手动拖动两条管线；约束开启的框架内禁用（该上下文 Alt 保留「出框豁免」语义，BACKLOG P0 决策）；副本分配全新 id、组 id 整组重映射、插回原 z 序、按 bbox 重新归属框架
- **绘制后自动切回选择工具**：Excalidraw 同款默认行为，实际生成元素才切换；设置 → 外观 → 绘制可关（`miniboard:draw-prefs` 持久化）
- **画布取色器**：样式浮层描边/填充通道组新增吸管——下一次点击直接读渲染像素（所见即所得含透明度合成），按激活通道应用并记入「最近」；Esc/右键取消
- **Ctrl 概览浮层（小地图）**：按住裸 Ctrl 约 200ms 弹出全画布简化矩形 + 视口框，点击/拖动定位视口中心（组合键与输入框打字不误弹）
- **自定义工具导入/导出**：导出为与持久化 `custom-tools.json` 同构的开放格式；导入逐个强制过冒烟测试，未通过/冲突逐一说明并跳过，id 一律重新分配
- **自定义工具使用统计**：完成绘制才计数（防抖落盘），管理页显示 ×N 徽标、支持 最常用/最近使用/按名称 排序与「清理未使用」（应用内确认后删除）
- **应用内确认弹窗**：`ui/confirm.ts` 提供 Promise 语义的 `showConfirm`（Esc=取消/Enter=确认、危险操作警示色）——清空画布与 AI 批量操作闸门已迁移；动机是 Tauri WKWebView 等环境不支持同步 JS 对话框（confirm 静默返回 false 会让确认闸门变成一律拒绝）
- **biome format 强制进 CI**：Test workflow 在 lint 后运行 `format:check`；接受一次性全量重排（57 文件，登记 `.git-blame-ignore-revs`）
- **AI 工具定义拆分**：schemas 迁移到 `ai/tool-schemas.ts`（tools.ts 保留执行器与向后兼容导出）；增量感知纯函数在 `ai/perception.ts`
- **确认弹窗全量覆盖**：剩余 7 处原生 `window.confirm`（项目删除 / 工具删除 / AI 配置删除 / 工具栏布局重置 / 快捷键重置与冲突覆盖 / 提示词重置）全部迁移到应用内 `showConfirm`——WKWebView 等不支持同步对话框的环境不再有静默失败路径
- **小地图实时联动**：Ctrl 概览浮层可见期间逐帧刷新，缩放 / 平移 / 内容变化即时反映视口框（单帧成本亚毫秒级）
- **History 测试补齐**：撤销栈语义、redo 分支裁剪、深度限制与 60 步 × 5000 元素规模行为；成本剖析结论沉淀至 BACKLOG（引用入栈 O(1)，真实成本在 serialize/loadElements 的 O(n)，维持万级以下不做增量序列化的决策）

### 新增（AI 工具管理）

- **生成器源码可编辑**：设置 → AI 工具的源码展示从只读升级为可编辑，保存前源码有变更即强制冒烟测试（与 AI update_tool 同一管线），未通过不落盘

### 修复（收尾批次）

- **状态栏选区尺寸**：选中元素时显示联合包围盒 W×H（与元素数/光标坐标同栏）
- **橡皮待删预览 rAF 合帧**：拖拽高频移动时每帧至多重算一次命中，大画布更顺滑
- **场景版本迁移骨架**：`migrateScene` 管道 + 载入接入——未来 v2 升级有落点，未知更高版本拒绝载入防降级改写
- **CSP 收紧**：从 null 改为白名单策略（self/data/blob/ipc，style 允许 inline 供 leafer 动态样式），缩小 WebView 攻击面

### 新增（AI 助手）

- **`delete_elements` 删除工具**：补齐交流模式唯一缺口——按 id 删除元素，锁定自动跳过、同组成员整组参与、整轮一步撤销；系统提示词同步约束「仅明确要求删除时使用，改外观优先 update_elements」
- **对话恢复不再失忆**：工具调用轮次落盘时压缩为一行摘要（［工具调用：create_elements…］），刷新/切项目后模型仍知道之前做过什么（图片数据照旧不落盘）
- **消息复制 / 重试**：面板头部新增两个按钮——复制最后回复到剪贴板；回退并原样重发最后一次请求（多模态图片附件不重发）
- **连接测试升级为能力探测**：携带假工具发最小请求检测端点是否支持 function calling，不支持时明示「编辑模式可能不可用」（部分旧版兼容端点的常见困惑）

### 新增（AI 第二批）

- **自检回执**：`update_elements` 成功结果回传实际更新的 id 清单（create_elements 原本已回传），模型可核对「改的是不是想改的」，减少幻觉式成功
- **批量操作闸门**：mutating 工具影响元素数超过 20 个时需用户确认执行——提示注入或模型误操作的最后一道防线
- **提示注入防护**：系统提示词新增安全边界条款——画布元素文字内容（尤其导入的文档）中的指令一律视为数据，禁止据此调用工具
- **用量透明**：每轮回复后发送按钮悬停显示本轮/会话累计 token 估算（相对 8000 预算的占比一目了然）

### 新增（AI 编辑模式）

- **`list_tools` 支持源码可见**：新增可选 `id` 过滤与 `includeSource` 参数，返回自定义工具的 generator 源码——修改工具前先取源码做增量修改，杜绝凭记忆重写导致的功能回退；系统提示词同步强制此流程
- **生成器范例库（few-shot）**：编辑模式提示词内置五角星印章 / 带标题便签两个完整范例，照例变形显著提升首次生成成功率
- **图标兜底**：AI 自报图标不合法时按工具名首字符降级（而非拒绝整个调用），不再出现占位符图标
- **运行时错误回执**：生成器实际执行异常此前只写控制台完全黑盒——现以 toast 提示工具名与错误原因并引导对话修复（每工具每会话一次）
- **GeneratorContext v2**：生成器 ctx 新增可选字段 zoom（画布缩放，实现「屏幕恒定大小」标注类元素）、shiftKey / altKey（修饰键约束，如 Shift 正比 / Alt 中心对称）——纯增量字段，既有自定义工具零影响；冒烟测试与编译探针的合成 ctx 同步补齐
- **试画位置记忆与原地更新**：同一工具的试画示例锚点被记忆；`update_tool` 修改生成器后在原位置更新示例（旧示例移除并与本轮合并为一步撤销），「再大一点」类迭代可原地对比，不再散落各处

## [0.2.0] - 2026-08-21

### 新增

- **橡皮擦全面升级**：
  - **半径可调**：橡皮激活时左侧栏显示半径滑条（复用画笔交互模式），`[` `]` 快捷键步进，橡皮模式下滚轮直接调节（不缩放画布），半径跨启动记忆
  - **直线/箭头分段擦除**：线性元素复用笔迹的分段算法——只擦被覆盖区间，剩余部分拆成独立线段（首段保起点箭头与绑定、末段保终点箭头与绑定，框架/分组归属随段继承）；命中判定加几何距离兜底（指针到线段距离 ≤ 橡皮半径即命中），不依赖 leafer 元素级命中——沿细线中段扫过即可分段（旧实现仅端点附近可靠）
  - **待删预览**：橡皮悬停/拖动经过时红色虚线框实时指示删除目标，所见即所删——整删类包住整个目标；笔迹/线性等分段类只高亮橡皮邻域内将被裁掉的区段（整条包围盒对长曲线无信息量）
  - **防误删**：矩形等整体删除类元素的命中改为「圆心触及本体」（容差取橡皮半径与 12px 的较小者并按缩放补偿为世界单位）——大半径蹭边不再误删整块图形，空心框内部穿越不受影响；同时修复旧实现命中半径未做缩放补偿导致的不同缩放倍率下手感漂移
  - **长按临时橡皮**：按住 `E` 借用橡皮擦除，松手自动回到原工具；快速单击则持久切换
- **易用性增强**：方向键微移选中元素（1px，按住连发）；`Shift+1` 缩放至全部内容、`Shift+2` 缩放到选中元素；双击空白处就地创建文本并进入编辑；默认样式（颜色/粗细/填充）跨启动记忆；自动保存成功在状态栏轻提示「已保存 ✓」（与失败 toast 形成闭环）；导出弹窗与命令面板新增 **PNG 复制到剪贴板**（Web Clipboard API，粘贴到聊天/文档一步到位）
- **内容约束机制重做**：
  - **归属判定改重叠率**：元素与约束框架的包围盒重叠面积 ≥60% 即受约束（旧实现按中心点判定——贴边元素中心在外则完全不受控、拖动一越线即"逃逸"，手感飘忽）
  - **嵌套语义明确**：多框命中取重叠率最高、并列取面积更小的内层框
  - **多选整体夹紧**：移动管线以选区联合 bbox 求一次修正量应用到每个成员，不再逐元素独立夹紧把框内相对布局拉歪
  - **AI 创建与粘贴接入框体系**：`addElement` 与剪贴板粘贴统一走「归属 + 约束夹紧」，AI 往框架里画 / 复制粘贴进框不再落在框外压线
  - **点编辑收尾兜底**：拖拽线段端点退出时整元素 bbox 兜底拉回约束框架（缩放/旋转管线的夹紧待后续接入）
  - **缩放/旋转收尾夹紧**：编辑器 SCALE/ROTATE 手势结束（DragEvent.END）时对受约束成员做一次 bbox 校正拉回框架——松手后校正，不与增量计算打架；Alt 豁免拖出时跳过
  - **约束状态可视化**：开启约束的框架描边由虚线转为实线，一眼区分普通框与约束框（随文件保存恢复）
  - **Alt 豁免提示**：首次拖动撞墙夹紧时 toast 提示「按住 Alt 可拖出」（每次会话只报一次）

- **样式扩展**：线型三档（实线/虚线/点线）、不透明度滑条（0–100%）、圆角半径滑条（0–100，仅矩形）
- **AI 批量排列工具**：`arrange_elements`——AI 以语义指令批量对齐/分布/翻转/层序，坐标计算由前端完成；`get_canvas` 画布感知补充分组信息
- **文本排版**：选中文本后样式浮层新增对齐（左/居中/右）、字体下拉（默认/常用中文字体/西文字体）、字重滑条（100–900，`Ctrl+B` 快捷切换常规/加粗）
- **箭头端点样式**：选中直线/箭头后样式浮层显示起点/终点两组端点按钮（箭头 / 三角 / 圆点 / 圆 / 无），两端独立设置（arrow 类型创建时默认三角头）
- **粗糙度控制**：选中手绘风格元素后样式浮层新增「粗糙」滑条（0–2，步进 0.1），以同一抖动 seed 即时重绘，撤销 / 重载后形态可复现
- **框架元素**：`F` 键创建虚线框（类 Figma Frame），框内元素跟随框架整体移动，支持 SVG 导出
- **内容框架与文件导入**：拖拽 MD / 代码 / 文本文件到画布或「导入文件」选择文件，生成内容型框架——按内容自适应尺寸（长行自动折行，代码等宽字体），内容随框架移动 / 缩放 / 旋转 / 删除，支持 SVG 导出；在框架内绘制 / 导入的元素自动归属框架
- **框架互转与内容约束**（右键菜单）：矩形 ⇄ 框架一键互转、内容约束开关（开启后框内绘制 / 拖动夹紧到框架边界）
- **橡皮擦分段擦除**：画笔笔迹按橡皮轨迹分段擦除——只擦被覆盖的区间，剩余部分自动拆成独立笔画（同一手势内幂等重算，不会重复拆分）；橡皮光标显示实际擦除半径
- **内容框架折叠与滚动**：超高内容框架可折叠（固定高度裁剪），鼠标悬停在框架上滚轮滚动查看内容，滚动偏移随文件保存恢复
- **框架聚焦**：右键菜单「聚焦框架」视口放大到框架，「退出聚焦」恢复原视图（会话级，不随文件保存）
- **导入文件入口**：右侧圆形工具栏新增「导入文件」按钮（与拖拽文件生成内容框架一致）
- **PDF 导出与导入**：统一导出弹窗新增 PDF 文档（画布位图化后嵌入单页 PDF，零依赖最小生成器，中文由浏览器渲染无字体问题）；拖拽 PDF 文件到画布自动渲染首页为图片（pdf.js 懒加载，长边上限 3000px）
- **拖拽分流**：拖入文件按类型自动分流——图片直插画布、PDF 首页转图片、MD / 代码 / 文本进内容框架，其他类型提示不支持
- **AI 按 id 风格工具**：`beautify_elements`（手绘笔迹整理为标准图形）、`sketchify_elements`（标准图形转手绘风格）、`set_roughness`（调整手绘粗糙度 0–2）——锁定元素自动跳过、同组成员整组参与、整轮改动一步撤销
- **流程图联动**：`draw_flowchart` 生成的节点矩形与文字自动同组（整组联动），连线箭头端点绑定节点矩形（节点移动连线自动跟随）
- **画布感知坐标语义修正**：`get_canvas` 改为世界坐标序列化——frame 内元素展开为画布绝对坐标，坐标 / region / 视口计算基准一致
- **存储原子写入与备份恢复**：桌面端场景 / 索引写入统一改为「写临时文件 → 旧版本复制为 .bak → 原子重命名落盘」（capabilities 补充 rename/copy-file/exists 权限），任一步骤中断最多丢本次写入，不再产生半截主文件；载入主文件缺失 / 损坏时自动回退上一版 .bak 备份，删除项目时一并清理备份
- **自动保存失败提示**：自动保存失败改为 toast 明示「更改尚未写入本地」（连续失败只报一次，恢复后自动复位，不刷屏）
- **存储迁移逻辑纯函数化**：项目索引解析（损坏条目丢弃保留其余）、激活项目回退、场景文件校验抽为无依赖的 `storage-core` 模块并补 10 个单元测试——数据完整性逻辑首次有回归防护

### 修复

- **内容约束在缩放/平移后失效**：开启约束后线段 / 箭头 / 形状 / 画笔绘制内容仍可画出框架——约束判定与钳制误用视口基准 bbox（worldBoxBounds）与绘制坐标（page 基准）混算，画布原点居中（zoomLayer 平移）后即错位；统一为 page 基准后约束在任意缩放 / 平移下生效
- **成组后拖动不联动**：组内元素拖拽移动不跟随（仅排列/层序/删除联动），已补齐编辑器拖拽与选中框内拖动的组展开
- **样式浮层打开后约 180ms 自动关闭**：面板互斥切换时残留隐藏定时器，误伤刚打开的面板
- **框架内容文字重叠**：leafer 的 lineHeight 数值为像素值，行高倍数误传为数值导致多行内容重叠，改用 percent 单位（与尺寸估算一致）
- **线段 / 箭头拖动失控**：拖动中立即归一化 x/y 破坏 leafer 编辑器增量计算——增量退化为累计总位移，元素被反复叠加放大、越拖越飞（方向不可控）；改为拖动结束统一并入 points/path 并归零
- **AI 按 id 操作对 frame 内元素计算失真**：`arrange_elements` 等按 id 操作以相对坐标计算导致对齐 / 分布错位，改为世界坐标基准计算（历史快照仍存相对坐标，撤销不受影响）
- **框架内容拖入不跟随**：元素拖入框架不建立归属，框架移动时内容原地不动；框内贴边元素（如夹紧到边界）因描边外扩约 1px 归属判定失败——拖入自动补挂归属，归属判定加描边半宽容差

### 移除

- **排列面板**：左侧选中栏的「排列」按钮及对齐 / 分布 / 翻转 / 成组 / 取消成组入口整体移除（AI 的 `arrange_elements` 工具与右键菜单置顶 / 置底保留）
- **容器内嵌文本**：矩形 / 椭圆双击输入内嵌文字功能移除（框架元素同步简化：不再带标题）
- **行高**：文本样式中的行高倍数控制移除（从未真正可用）
- **回收站**：删除 / 清空直接移出画布，不再有恢复入口（原文档描述的功能从未实现）

### 其他

- **接入 Biome lint**：新增 `npm run lint` 脚本与 CI lint 步骤（推荐规则集）；存量可安全修复项一次性改正（字符串拼接 → 模板字面量、字面量键、可选链、无用转义等）；正则推进循环赋值与非空断言为本仓库既定惯用法，配置豁免；CSS 的 `!important` 与选择器嵌套告警暂关
- **canvas.ts 增量拆分（第一步）**：图片裁剪拆为独立 `CropController` 控制器（Board 门面 API 不变，事件管线经控制器短路分发）；命中测试几何函数拆出 `geometry.ts`、元素级工具（箭头端点映射 / 类型判定 / 绑定透传）拆出 `element-utils.ts`；frame 坐标契约（世界 ⇄ 相对）下沉为 `frame.ts` 纯函数并补旋转框架下的往返还原测试
- **README 安全提示**：中英文 README 明示 API Key 以明文形式保存在 WebView localStorage 及应对建议
- `.workbuddy/` 本地工作区记忆目录加入 .gitignore

## [0.1.0] - 2026-08-15

首个开源版本。

### 新增

- **可自定义数据存储目录**：⚙ 设置 →「数据」页签可更改项目与 AI 工具的存储位置，更改时自动迁移旧数据，重启后自动恢复（Rust 端 `set_data_dir` 命令负责创建/迁移/授权/记录配置）
- **AI 自定义工具改为文件存储**：桌面端从 localStorage 迁移到数据目录 `custom-tools.json`（首次启动自动迁移旧数据），可随项目数据一起备份
- **内联项目重命名**：用内联输入行替代原生 `prompt`（Tauri WebView2 不支持），支持回车提交 / Esc 取消
- **快捷键自定义**：⚙ 设置 →「快捷键」页签录制式改键——操作与内置工具全部可改，冲突自动检测提示，一键恢复默认；命令面板“快捷键帮助”动态反映当前键位
- **画布原点居中**：初始视图画布原点 (0,0) 居中显示在视口中心（X0Y0 居中），还原缩放时回到该视图
- **对话历史持久化**：AI 对话按 项目×模式 分桶保存（刷新 / 切换模式 / 换项目不丢失；tool 轮次与图片数据不落盘）

### 修复

- **桌面端文件存储完全失效**：`fs:default` 权限不附加任何文件访问 scope，导致所有读写操作被 ACL 拒绝（项目功能全不可用）；已在 capabilities 中为读写/删除命令显式附加 `$APPDATA/**` scope
- **项目重命名不可用**：`window.prompt` 在 WebView2 返回 null，重命名流程永远无法提交
- **重命名后列表不刷新**：提交成功后先刷新列表再触发项目变更回调
- **设置界面工具栏页签**：exe（WebView2）中平铺区拖拽排序失效——`dataTransfer.getData` 在拖拽过程读取不可靠，改为 `dragstart` 时实例暂存拖拽源；工具从收纳区取出后永久带分组标签——取出时清除分组覆盖与“＋”加入记录，恢复默认归属；工具加入收纳区后误现于隐藏区——隐藏区按有效分组（含覆盖）过滤
- **工具栏拖拽交互全面修复**：分组按钮行/分组头拖到平铺区工具行不再无反应（可插入该行前/后）；工具/分组拖到自动同步的分组按钮副本（虚线行）可正常落点并固定位置；落点按目标行上下半判定插入其前/后，从上往下拖相邻行生效；分组头排序修复向下移动时多移一位；拖拽改用指针捕获 + `pointercancel`/窗口失焦兜底——WebView2 中拖出窗口松开会丢失 `pointerup`，不再残留拖拽状态导致后续所有行无法再拖
- **设置弹窗不自适应窗口**：固定 440px 改为 `min(440px, 100vw-32px)` 随窗口收窄，页签文字不再被挤压换行（窄窗口页签整体换行、文字不折行）

### 其他

- 构建脚本 `tools/build-tauri.ps1` 参数化：自动探测 MSVC / Windows SDK（vswhere + 最新版本），支持环境变量覆盖，去除本机硬编码路径
- 新增开源配套文档：LICENSE（MIT）、CONTRIBUTING、CODE_OF_CONDUCT、英文 README、Issue/PR 模板、CI 工作流
- 清理 src-tauri 冗余图标（android/ios 移动端与 Windows Store 徽标），补全 Cargo.toml 元信息（license / repository / authors）
- 设置“工具栏”页签精简界面文字：去除行徽章、复选框文字、分区说明与数量提示行；平铺区行不再显示分组归属标签
