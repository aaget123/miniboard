# 更新日志

本项目的所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- **样式扩展**：线型三档（实线/虚线/点线）、不透明度滑条（0–100%）、圆角半径滑条（0–100，仅矩形）
- **AI 批量排列工具**：`arrange_elements`——AI 以语义指令批量对齐/分布/翻转/层序，坐标计算由前端完成；`get_canvas` 画布感知补充分组信息
- **文本排版**：选中文本后样式浮层新增对齐（左/居中/右）、字体下拉（默认/常用中文字体/西文字体）、字重滑条（100–900，`Ctrl+B` 快捷切换常规/加粗）
- **箭头端点样式**：选中直线/箭头后样式浮层显示起点/终点两组端点按钮（箭头 / 三角 / 圆点 / 圆 / 无），两端独立设置（arrow 类型创建时默认三角头）
- **粗糙度控制**：选中手绘风格元素后样式浮层新增「粗糙」滑条（0–2，步进 0.1），以同一抖动 seed 即时重绘，撤销 / 重载后形态可复现
- **框架元素**：`F` 键创建虚线框（类 Figma Frame），框内元素跟随框架整体移动，支持 SVG 导出
- **内容框架与文件导入**：拖拽 MD / 代码 / 文本文件到画布或「导入文件」选择文件，生成内容型框架——按内容自适应尺寸（长行自动折行，代码等宽字体），内容随框架移动 / 缩放 / 旋转 / 删除，支持 SVG 导出；在框架内绘制 / 导入的元素自动归属框架
- **框架互转与内容约束**（右键菜单）：矩形 ⇄ 框架一键互转、内容约束开关（开启后框内绘制 / 拖动夹紧到框架边界）
- **橡皮擦分段擦除**：画笔笔迹按橡皮轨迹分段擦除——只擦被覆盖的区间，剩余部分自动拆成独立笔画（同一手势内幂等重算，不会重复拆分）；橡皮光标显示实际擦除半径
- **内容框架折叠与滚动**：超高内容框架可折叠（固定高度裁剪），鼠标悬停在框架上滚轮滚动查看内容，滚动偏移随文件保存恢复
- **框架聚焦**：右键菜单「聚焦框架」视口放大到框架，「退出聚焦」恢复原视图（会话级，不随文件保存）
- **导入文件入口**：右侧圆形工具栏新增「导入文件」按钮（与拖拽文件生成内容框架一致）

### 修复

- **成组后拖动不联动**：组内元素拖拽移动不跟随（仅排列/层序/删除联动），已补齐编辑器拖拽与选中框内拖动的组展开
- **样式浮层打开后约 180ms 自动关闭**：面板互斥切换时残留隐藏定时器，误伤刚打开的面板
- **框架内容文字重叠**：leafer 的 lineHeight 数值为像素值，行高倍数误传为数值导致多行内容重叠，改用 percent 单位（与尺寸估算一致）
- **线段 / 箭头拖动失控**：拖动中立即归一化 x/y 破坏 leafer 编辑器增量计算——增量退化为累计总位移，元素被反复叠加放大、越拖越飞（方向不可控）；改为拖动结束统一并入 points/path 并归零

### 移除

- **排列面板**：左侧选中栏的「排列」按钮及对齐 / 分布 / 翻转 / 成组 / 取消成组入口整体移除（AI 的 `arrange_elements` 工具与右键菜单置顶 / 置底保留）
- **容器内嵌文本**：矩形 / 椭圆双击输入内嵌文字功能移除（框架元素同步简化：不再带标题）
- **行高**：文本样式中的行高倍数控制移除（从未真正可用）
- **回收站**：删除 / 清空直接移出画布，不再有恢复入口（原文档描述的功能从未实现）

## [0.1.0] - 2026-08-15

首个开源版本。

### 新增

- **可自定义数据存储目录**：⚙ 设置 →「数据」页签可更改项目与 AI 工具的存储位置，更改时自动迁移旧数据，重启后自动恢复（Rust 端 `set_data_dir` 命令负责创建/迁移/授权/记录配置）
- **AI 自定义工具改为文件存储**：桌面端从 localStorage 迁移到数据目录 `custom-tools.json`（首次启动自动迁移旧数据），可随项目数据一起备份
- **内联项目重命名**：用内联输入行替代原生 `prompt`（Tauri WebView2 不支持），支持回车提交 / Esc 取消
- **快捷键自定义**：⚙ 设置 →「快捷键」页签录制式改键——操作与内置工具全部可改，冲突自动检测提示，一键恢复默认；命令面板“快捷键帮助”动态反映当前键位
- **画布原点居中**：初始视图画布原点 (0,0) 居中显示在视口中心（X0Y0 居中），还原缩放时回到该视图
- **对话历史持久化**：AI 对话按 项目×模式 分桶保存（刷新 / 切换模式 / 换项目不丢失；tool 轮次与图片数据不落盘）

### 修复

- **桌面端文件存储完全失效**：`fs:default` 权限不附加任何文件访问 scope，导致所有读写操作被 ACL 拒绝（项目功能全不可用）；已在 capabilities 中为读写/删除命令显式附加 `$APPDATA/**` scope
- **项目重命名不可用**：`window.prompt` 在 WebView2 返回 null，重命名流程永远无法提交
- **重命名后列表不刷新**：提交成功后先刷新列表再触发项目变更回调
- **设置界面工具栏页签**：exe（WebView2）中平铺区拖拽排序失效——`dataTransfer.getData` 在拖拽过程读取不可靠，改为 `dragstart` 时实例暂存拖拽源；工具从收纳区取出后永久带分组标签——取出时清除分组覆盖与“＋”加入记录，恢复默认归属；工具加入收纳区后误现于隐藏区——隐藏区按有效分组（含覆盖）过滤
- **工具栏拖拽交互全面修复**：分组按钮行/分组头拖到平铺区工具行不再无反应（可插入该行前/后）；工具/分组拖到自动同步的分组按钮副本（虚线行）可正常落点并固定位置；落点按目标行上下半判定插入其前/后，从上往下拖相邻行生效；分组头排序修复向下移动时多移一位；拖拽改用指针捕获 + `pointercancel`/窗口失焦兜底——WebView2 中拖出窗口松开会丢失 `pointerup`，不再残留拖拽状态导致后续所有行无法再拖
- **设置弹窗不自适应窗口**：固定 440px 改为 `min(440px, 100vw-32px)` 随窗口收窄，页签文字不再被挤压换行（窄窗口页签整体换行、文字不折行）

### 其他

- 构建脚本 `tools/build-tauri.ps1` 参数化：自动探测 MSVC / Windows SDK（vswhere + 最新版本），支持环境变量覆盖，去除本机硬编码路径
- 新增开源配套文档：LICENSE（MIT）、CONTRIBUTING、CODE_OF_CONDUCT、英文 README、Issue/PR 模板、CI 工作流
- 清理 src-tauri 冗余图标（android/ios 移动端与 Windows Store 徽标），补全 Cargo.toml 元信息（license / repository / authors）
- 设置“工具栏”页签精简界面文字：去除行徽章、复选框文字、分区说明与数量提示行；平铺区行不再显示分组归属标签
